# 전성빈 AGILE-SEC 구현 계획

작성: 2026-09-07. 상태: 구현 계획, 공용 변경 승인 문서 아님.
기준 main: ae43fe9ae119629cbfc9cbaa1f55c15babede926
현재 브랜치: security/agile-chat-write-gate
현재 HEAD: b9332f82af31f81d963ab7c5653bfd1a48496ade
상세 이력: AGILE_SEC_CHANGELOG.md

## 1. 목적과 원칙
LLM 결과는 제안으로 처리하고, 실제 사용자 권한과 명시적 승인을 규칙 기반으로 검증한 뒤 저장/수정/발행한다.
AGENTS.md와 최신 팀 분담을 적용한다. 승인 전 공용·타 담당 파일을 수정하지 않는다.
실행 중 앱의 DB·계정·API 키·기존 파일을 보존한다. 실사용 저장소로 공격 테스트를 하지 않는다.
생성 승인, 기존 내용 수정 승인, 담당자 배분, 개발 착수 승인을 별도 동작으로 정의한다.
후보 검증/지문 비교는 승인이 아니다. 모델이나 요청의 approved/actor/team 값은 권한의 근거가 아니다.

## 2. 범위
### 우선 구현
- task_generator.py: 기존 자동 수정 차단 유지, 신규 후보 검증, 기능별 보안 항목 누락 검사.
- idea_chat.py 및 Chat 전용 보조 코드: 저장 후보 처리, 파싱 실패 시 쓰기 후보 차단.
- Agile/Chat 전용 테스트: 정상·악성 결과·오류·중복·충돌 검증.
- 거절 태스크가 실행 가능한 제안/후속 분석 근거로 재사용되지 않도록 해당 도메인 경계에서 처리.

### 협의 후 연결
- pipeline_runner.py: A와 공통 소유. run_idea_chat 메모 관련 구간과 A의 입력 경계 작업 구간 분리.
- rest_handler.py, task_coordinator.py, 프런트: 담당자와 정확한 변경 범위 확인 필요.
- core/state.py가 새 후보 필드 보존을 위해 필요할 수 있음. 먼저 기존 채널을 점검하고 필요성이 확인되면 별도 승인 대상으로 추가.
- 전용 보조 파일 추가도 다른 도메인 호출 계약을 바꾸면 협의한다.

### 별도 범위 확정
- Wiki·문서 발행은 초기 사용자 임무에 포함됐지만 최신 팀 분담에는 명시되지 않았다. 기존 후보 모듈은 보존하고 실제 발행 변경은 범위 확인 후 진행.
- Dev Tracking knowledge/persistence/doc_updater, PM/SA 노드, 공용 LLM 유틸리티는 임의 수정하지 않는다.
- 배분 로직 전반 개선, 공유 스냅샷 기능, 원격 인증 서버 복구는 현재 구현의 독립 후속 과제. 승인 경계를 우회하는 부분이 발견되면 영향과 최소 변경안을 보고한다.

## 3. 기준선
완료: 태스크 생성기 즉시 create/update 제거, 제안 응답, 중복 검사, 파싱 오류 구분. 7개 테스트, b9332f8 push.
준비: 메모/문서 불변 후보와 변경 지문, 신규 테스트 12개. 아직 런타임 미연결·미커밋.
현 UI는 새 task_proposals/update_proposals를 처리하지 않는다. 사용자 승인 후 저장하는 전체 흐름은 미완료.
현재 Chat의 notes_to_add는 Runner가 자동 저장한다. 후보 모듈 추가로 보호됐다고 보지 않는다.
list_tasks는 init_tasks_db를 호출하므로 태스크 쓰기 차단과 DB 초기화 부작용을 구분한다.

## 4. 구현 패키지
### P0 — 기준선과 테스트 격리
- branch/HEAD/status, 로컬 미커밋 파일, 적용 규칙 확인. 원격 조회 필요 시 수행하되 자동 merge/rebase하지 않음.
- 기존 19개 테스트 재실행. 모듈 import와 DB 초기화 부작용을 조사한다.
- 통합 테스트는 tmp 디렉터리 SQLite, 테스트 전용 엔진/세션, 가짜 사용자/프로젝트 사용.
- 단위 테스트는 실제 모델 대신 고정 응답. 요청·저장·네트워크 함수를 spy로 검사.
완료: 실제 DB/외부 쓰기 없이 재현 가능한 테스트 환경과 현재 실패 목록.

### P1 — 태스크 제안 검증
- run_task_generator는 계속 신규/기존 태스크를 쓰지 않음.
- TaskGeneratorOutput 파싱 후 값·길이·필수 필드·feature_ref를 검증.
- 허용 enum/상한은 기존 정상 데이터와 화면 기대값을 확인한 뒤 명시. 정상 데이터를 임의로 배제하지 않음.
- 기존/배치 내부 중복 차단 유지. 존재하지 않거나 미할당이 아닌 ID의 수정 제안 거부.
- 동일 제목에 다른 내용, 의미상 중복, 빈 feature_ref의 한계를 별도 기록.
- 모델 summary/thinking은 사실·승인 증거로 취급하지 않음.
완료: 가짜 승인·잘못된 필드·잘못된 ID 응답에도 쓰기 0회, 정상 후보와 변경 전후 차이 반환.

### P2 — 필수 보안 규칙
- 팀이 검토할 표: 기능 유형, 적용 조건, 안정적 requirement/security ID, 필요한 태스크, 증거 기준.
- 인증: 로컬 비밀번호 인증이면 해싱; JWT 사용 시 JWT 검증; 역할 구분 시 RBAC; 로그인 rate limit와 인증 테스트.
- 업로드: 크기 제한/MIME 검증; 압축 해제가 있는 경우 압축 폭탄 방어.
- Webhook: 서명·재전송 방지·secret 미설정 시 차단.
- 이 조건은 팀 제공 예시를 구체화하는 초안. 모든 프로젝트에 무조건 적용하지 않음.
- 기능 적용 여부는 검증된 요구사항/합의된 설정에서 가져온다. 오염된 설계가 기능 자체를 숨기는 문제는 PM/SA와 입력 계약 협의.
- 제목 키워드로 충족 판정하지 않음. 기능·보안 ID와 검증 가능한 기준을 대조.
- 누락 시 검토 필요 반환 또는 고정 템플릿 보완 후보 생성. 보완 후보도 승인 전 저장 금지.
- 기존 중복 정책(feature_ref 하나로 차단)이 같은 기능의 여러 보안 태스크를 지우지 않는지 검증. 별도 보안 식별 계약 필요.
완료: 필수 항목 누락/이름만 위장/규칙 제거 지시에 흔들리지 않고, 미적용 기능은 과잉 생성하지 않음.

### P3 — 거절 태스크 격리
- LLM의 실행 가능한 컨텍스트에서 거절 제목/본문/지시를 제외하는 방안 구현.
- 서버 측 중복 방지용 ID/ref/정규화 식별 정보는 유지. 전부 삭제하면 거절 태스크 재생성이 가능하므로 구분.
- 현재 COVERED가 모든 상태를 모델에 전달하는 지점부터 수정.
- 후속 분석·RAG 수집 경로를 추적. DEV-SEC 소유 파일 변경은 계약과 재현 테스트를 전달하고 별도 승인.
- 기존에 인덱싱된 데이터 정리/재색인은 파괴적·범위 외 작업으로 보고 별도 결정.
완료: 도메인 입력/출력 격리와 재생성 방지가 함께 동작. RAG 전역 차단은 담당자 검증 전 미완료 표시.

### P4 — 메모 후보 처리
- 구조화 출력의 후보만 검증. 파싱 실패의 자유 형식 fallback에서는 답변은 제공할 수 있으나 쓰기 후보는 반환하지 않음.
- 사용자 요청 없는 메모 생성을 프롬프트만으로 차단하지 않음. 모든 후보는 명시적 저장 전 미저장 상태.
- 현 정규화가 긴 내용을 잘라낸 후 검증하는 순서를 피한다. 검토할 정확한 본문을 검증하고 보존.
- 단순 저장 키워드, 이전 대화 속 저장 요청, 인용문을 승인으로 취급하지 않음.
- 새 후보가 LangGraph state/Runner에서 유실되지 않도록 반환 경로 확인. 공용 state/Runner 변경은 합의 후 수행.
완료: 노드 수준 정상/실패 테스트. 실제 저장 차단 완료는 P5 연결 후에만 선언.

### P5 — 승인과 DB 쓰기 연결 (승인 범위 확인 후)
- 서버가 보관한 후보에 예측 불가능한 proposal ID를 부여하고 인증된 사용자·프로젝트와 연결.
- 저장 전 검토 대상 필드: 신규/수정/메모 구분, 정확한 본문, 대상 ID, 팀/프로젝트, 기준 버전.
- 클라이언트는 승인할 ID/선택 정보만 전송. 수정한 후보는 재검토하며 원본을 요청 본문으로 대체하지 않음.
- 서버가 권한·소유권·유효기간·선택·사용 여부·현재 대상 버전을 검증.
- 사용자/팀 없는 기존 로컬 프로젝트는 소유권을 추측하지 않음. 로컬 전용 정책 또는 명시적 소유권 연결 절차를 합의.
- 초안 보관: 초기 로컬 단일 프로세스 메모리 방식을 제안(미승인 초안 DB 저장 없음). 재시작 시 무효화하고 재검토 안내. 멀티프로세스/영구 초안은 공용 저장 설계 추가 필요.
- 상태: pending → executing → succeeded 또는 failed/outcome_unknown, cancelled/expired. UI에 DB의 개발 상태와 혼동시키지 않음.
- 로컬 트랜잭션 내 선택 배치 전체 검증 후 반영. 승인 소모와 결과 기록의 원자성은 저장 구조에 맞춰 설계하며 프로세스 장애 시 중복도 고려.
- 기존 태스크 적용은 현재 상태·버전 조건부 UPDATE. 변경 건수 불일치 시 conflict, 자동 덮어쓰기 금지.
- commit 성공 후 실제 ID/저장 개수 반환. 실패한 화면 임시 항목은 미저장으로 표시.
- API에 직접 접근해 UI 승인 단계를 우회하는 경로도 검사. HTTP 요청만으로 실제 클릭을 암호학적으로 증명할 수 있다고 주장하지 않음.
완료: 사용자 선택 저장, 타 사용자 거부, 위조/만료/재전송/중복/충돌/commit 실패 테스트 통과.

### P6 — Wiki·문서 (범위 및 호출자 합의 후)
- GitHub 공식 문서로 현재 Wiki 쓰기 인터페이스 지원을 확인한 뒤 구현 방식을 결정.
- 최종 Markdown 생성 → 대상/기준 버전 조회 → 미리보기 → 승인 → 정확히 같은 본문 발행.
- 대상 owner/repo/mode/page/기준 버전이 바뀌면 승인 무효. 승인 후 LLM 재생성 금지.
- 직접 발행/동기화/태스크 실행/Dev Tracking 연계를 빠짐없이 연결. 기존 PM 승인 범위는 DEV-SEC와 합의.
- 라벨 생성도 쓰기이며 모든 쓰기 전 검증. 401/403/5xx를 '없음'으로 간주해 생성하지 않음.
- 대상 문서의 외부 변경 감지, 중복 요청 처리. 원격 API가 조건부 쓰기를 지원하지 않으면 경합 한계를 명시.
- timeout은 결과 불명일 수 있으므로 재조회 없이 맹목적으로 재발행하지 않음.
완료: mock에서 모든 승인/거부 경로, 이후 사용자 지정 테스트 저장소에서만 실제 연동 검증.

## 5. 회귀 테스트 매트릭스
| 유형 | 정상 사례 | 차단 사례 | 검증할 부작용 |
| --- | --- | --- | --- |
| 태스크 | 검증된 신규/수정 후보 | 승인 위조, 잘못된 ref/ID, 상태 조작 | 승인 전 create/update 0회 |
| 보안 규칙 | 적용되는 필수 항목 포함 | 누락, 이름만 위장, 기능/규칙 제거 지시 | 오류/검토 필요, 무조건 저장 없음 |
| 거절 격리 | 정상 태스크 컨텍스트 유지 | 거절 본문 재전달, 동일 항목 재생성 | LLM 입력/RAG 경계와 쓰기 검사 |
| 메모 | 정확한 후보와 선택 저장 | 인용문 저장 명령, fallback 후보, 타 세션 | 승인 전 commit 0회 |
| 승인 | 허용 사용자·대상·내용 | 위조/만료/재사용/권한 상실/충돌 | 원자성·중복·실패 결과 |
| 문서 | 승인된 최종 본문 발행 | 대상 변경, 조회 오류, timeout 재발행 | 외부 쓰기 호출 횟수/인자 |

## 6. 단계별 종료 기준과 증거
- 단위 완료: 해당 규칙 테스트 통과. 새 모듈 미존재 수집 실패는 공격 재현 증거가 아님.
- 로컬 통합 완료: API/Runner/state/UI와 임시 DB 정상/실패 시나리오 통과.
- 운영 연계 완료: 원격 인증 서버 복구 후 실제 계정/팀 권한 및 허가된 GitHub 테스트.
- 승인된 변경 외 파일 없음, diff/check/status와 기존 파일 보존 확인.
- AGILE_SEC_CHANGELOG.md에 파일·함수·테스트·한계·승인 범위 기록.
- 명시적 요청 없이 commit/push/PR/merge하지 않음. 승인된 커밋은 기능 단위로 분리하고 미완료 연결 상태를 메시지에 표기.

## 7. 예상 결과와 남는 한계
- 생성 버튼은 제안을 보여주며 승인한 항목만 저장. 기존 내용은 비교·승인·충돌 검사 후 적용.
- 일반 대화/인용문은 실제 메모 저장으로 이어지지 않음. 모델 답변과 저장 완료 알림 분리.
- 거절 태스크는 실행 근거에서 제외하되 중복 방지 식별 정보 유지.
- 보안 태스크는 합의된 조건과 규칙으로 검사. 상위 입력 증거 위조까지 혼자 해결했다고 주장하지 않음.
- 문서 발행은 범위 합의 이후 완료 대상. 모듈 준비만으로 발행 보호 완료를 선언하지 않음.
- 인증 서버 장애 동안 mock 테스트는 가능하지만 실제 권한 통합은 완료 처리 불가.

## 8. 우선 실행 순서
P0 → P1 → P2/P3 → P4 → 합의된 P5 → 범위 확정된 P6.
협의 대기 중에는 담당 영역의 순수 검증과 회귀 테스트를 진행한다. 연결 없는 후보 모듈을 무한 추가하지 않고 실제 사용 가능한 한 단계의 종료 기준을 우선한다.
다음 실제 코드 변경은 P0 기준 테스트 재실행 후 P1의 태스크 후보 검증과 P3의 거절 컨텍스트 분리를 작은 단위로 수행한다.


## 9. 진행 상태 갱신 — 2026-09-08
- 사용자의 P0~P5 진행 요청에 따라 구현 중. 위 초기 계획의 “미연결” 항목은 당시 기준이며 현재 증거는 AGILE_SEC_CHANGELOG.md 변경006~016을 함께 확인한다.
- 현재 검증: Agile36, Chat28, 태스크/메모 프런트12, 기존 DEV coordinator1 통과. 최종 프런트 빌드 및 diff --check 통과.
- P1 후보 검증, P2 규칙, P3 생성기 거절 제외, P4 후보 처리, P5 승인/수동 API·프로젝트 연결은 구현이 존재하며 개별 검증 완료. 전체 통합 완료로는 처리하지 않음.
- 이번 완료 묶음: 수동 태스크 정상 수락/거절과 필드 권한, 화면 인증/검토 버전/삭제 결과, 동일 DB 설정 연결. 파일·실패 재현·현재 테스트는 변경014~016에 기록.
- 다음 묶음: 배분 경로를 서버 권한/검증된 후보/명시적 적용으로 연결. 이어서 공용 호출자·실제 화면·P3 후속 지식 경로를 감사하고 P0~P5 전체 완료 여부를 판단한다.
- P6은 Dev Tracking 담당자와 합의 전 구현 보류. 진행률 숫자는 추정이며 테스트 개수나 부분 완료를 전체 완료로 환산하지 않는다.


### 2026-09-08 추가 진행 — 배분 및 브라우저 태스크 승인
- P5 배분 경로: 제안 생성·서버 권한/상태 재검증·선택 일괄 적용 연결. Agile49 및 프런트16개 테스트 통과.
- 실제 브라우저에서 배분 승인 전 쓰기 없음, 선택1개만 적용, 동시 수정 충돌 보존, JWT 필수 후보 제외 시 저장 비활성화 및 포함 승인 후 정확한2개 저장 확인.
- 근거와 파일 상세는 변경017~018. 미완료: Memo 실제 화면 통합, 공용 호출자 회귀, P3 후속 분석/지식 입력 감사, 최종 요구사항별 완료 판정. P6 계속 보류.


### 2026-09-08 추가 진행 — 메모 승인 화면
- 변경019: 지연된 메모 승인 응답의 계정·팀·프로젝트 경계, 실패 후 재승인 차단 구현. 프런트20/Chat28 통과, 최신 전체 빌드 통과.
- 메모 목록의 계정 전환과 조회 실패 상태, 실제 MemoManager 브라우저 통합, 공용 호출자/거절 태스크 후속 경로를 다음에 확인. P6 보류 유지. 전체 완료 판정은 아직 하지 않음.
